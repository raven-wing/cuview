[project]
name = "worker"
version = "0.1.0"
description = "Cloudflare Worker: ClickUp OAuth callback handler"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
